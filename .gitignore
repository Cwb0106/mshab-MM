# 1. 忽略根目录下的所有文件和文件夹
/*

# 2. 不要忽略 .gitignore 文件本身（非常重要，否则你下次无法修改规则）
!.gitignore

# 3. --- 白名单区域 (只允许以下文件夹) ---

# 允许 GAPartMobile 文件夹
!/GAPartMobile/

# 允许 mshab 子文件夹
!/mshab/

# 注意：如果这两个文件夹内部还有你想忽略的文件（比如 __pycache__），
# 你可能还需要额外添加规则，例如：
# __pycache__/
# *.pyc